Removed the `routing` wrapper from the `network` values schema so `network.ingress` and `network.gateway` are direct siblings, updated convention files to agentics template, and completed stateless-svc NP schema.

- `stateless-svc/values.yaml`, `stateless-svc/templates/routing-ingress.yaml` (renamed `ingress.yaml`), `stateless-svc/templates/httproute.yaml`, `stateless-svc/templates/NOTES.txt`, `stateless-svc/MIGRATION.md`: `network.routing.ingress` → `network.ingress`, `network.routing.gateway` → `network.gateway`; `routing` wrapper removed after design review
- `CLAUDE.md`, `AGENTS.md`: updated to agentics template v0.1.0 structure; added interaction parameters, critical constraints, session file identity, entry format, tech-debt format, convention placement, initialization; corrected stale gh-pages publishing reference to GHCR OCI
- `.dev/sessions/`: migrated from flat `sessions.md` to per-session files; historical entries backfilled with `T000000` placeholder timestamps
- `stateless-svc/templates/networkpolicy.yaml`: `cidrs[].host` → `cidrs[].cidr`; made `port` optional on all rule types (cidrs, pods inbound/outbound); made `labels` optional on pods rules (omitting generates namespace-only selector, covering the kube-backup/operator namespace use case); fixed double-range labels rendering bug (was only correct for list-of-maps, not plain maps); inline comments added for `allowSameNamespace` and `allowDns`
- `stateless-svc/values.yaml`: updated examples to show all combinations (namespace-only, labels-only, full labels+port); updated cidr examples to show port-optional usage; `service.port` default removed (now falls back to `container.port` in templates); dead `portName` field removed
- `stateless-svc/templates/service.yaml`, `stateless-svc/templates/httproute.yaml`: `service.port` falls back to `container.port` when not set; removes the need to duplicate the port value in the common case where service and container ports match
- `stateless-svc/MIGRATION.md`: documented optional ports, optional labels on pods, namespace-only selector pattern, and the `service.port` default change with migration note
