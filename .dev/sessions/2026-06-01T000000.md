Switched chart publishing from GitHub Pages to OCI artifacts on GHCR, added cron-job chart, and created initial devctx scaffolding.

- `Jenkinsfile` / pipeline: switched from `helm repo index` + git push to `helm push` OCI; removed `apk add` (blocked by runAsUser: 1000); replaced `yq` with `grep`/`awk`; credential reverted to PAT after GitHub Apps proved unable to push to GHCR
- `cron-job/`: added chart (from argo-charts, bumped to 1.2.1) with `concurrencyPolicy: Forbid`, job history limits, camelCase values, lint fixes
- `README.md`, `DEVELOPMENT.md`: updated for OCI usage
- `.dev/Makefile`: removed (irrelevant after OCI switch)
- `CLAUDE.md`, `AGENTS.md`, `.github/copilot-instructions.md`, `DEVELOPMENT.md`: initial devctx scaffolding
- `.dev/roadmap.md`, `.dev/tech-debt.md`, `.dev/sessions/`: initial working documents
